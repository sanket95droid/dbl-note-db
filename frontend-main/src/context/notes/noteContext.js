// context holds states and provide them to any component irrespective of heirarchy

import { createContext } from "react";

const noteContext = createContext(); // creating a new context

export default noteContext;